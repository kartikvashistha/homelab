name: k8s-apps
description: A pulumi program to deploy custom apps into a k8s cluster
runtime: go
config:
  pulumi:tags:
    value:
      pulumi:template: go

  kubernetes:context:
    value: 
      type: string
      description: Kubernetes context to use when installing & configuring objects within it
